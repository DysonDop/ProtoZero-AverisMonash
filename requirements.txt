# Core pipeline — no cloud dependency. `--no-llm` runs on exactly this set.
pydantic>=2.7
PyYAML>=6.0
rapidfuzz>=3.9
PyMuPDF>=1.24
python-docx>=1.1
openpyxl>=3.1

# API + serving
fastapi>=0.111
uvicorn[standard]>=0.30

# Azure — optional at runtime; the deterministic path never imports these.
azure-ai-documentintelligence>=1.0.0
azure-identity>=1.17
openai>=1.40

# Dev
pytest>=8.2
httpx>=0.27
